These source packages:

	apache-tomcat
	apr
	tomcat-native

should be installed into (copied or linked into) ~forge/rpmbuild/SOURCES
s.t. they can be built and delivered into the appliance distribution.