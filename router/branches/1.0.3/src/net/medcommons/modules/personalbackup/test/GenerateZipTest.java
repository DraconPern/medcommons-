package net.medcommons.modules.personalbackup.test;

import junit.framework.TestCase;

public class GenerateZipTest extends TestCase{
	public void testZip(){
		// Currently a noop.
		// Need to run tests from inside working gateway.
		// Possibly generate fake data on the fly to generate zips so that test could be independent of gateway?
	}

}
